# Idempotency

If an operation can be retried, repeated execution must not duplicate unintended effects. Prefer durable operation keys, unique constraints, provider idempotency, or atomic transitions.
