# Correlation context

Generate or accept a safe request correlation ID at entry points.

Propagate it through:
- logs
- downstream HTTP calls
- queued jobs
- webhook processing
- async workflows

Do not use correlation IDs as authentication or authorization evidence.
