# Tracing and OpenTelemetry

Trace distributed causal paths across HTTP, queues, DB and external APIs when diagnostic value justifies it.

Propagate trace/correlation context across asynchronous boundaries.

Add attributes that help explain behavior, but avoid sensitive payloads and unbounded values.

Instrument major boundaries first. Do not create spans for every trivial helper.
