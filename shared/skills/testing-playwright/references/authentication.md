# Playwright authentication

Use secure test-only storage state or programmatic authentication when appropriate.

Keep dedicated coverage for login UI itself.

Never commit real production credentials or session state.

Use dedicated test users/data.
