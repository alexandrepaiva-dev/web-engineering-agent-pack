# Flakiness

Fix the underlying synchronization/data/isolation cause.

Do not solve flakiness with arbitrary sleeps or weakened assertions.

Investigate navigation races, animation, async work, stale shared data, parallel collisions, missing awaits and unstable selectors.
