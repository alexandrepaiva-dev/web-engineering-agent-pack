# Fixtures

Use fixtures for reusable environment capabilities such as authenticated contexts, seeded users, API clients and cleanup.

Do not hide important test intent behind huge fixtures.

Avoid global mutable state shared by parallel tests.
