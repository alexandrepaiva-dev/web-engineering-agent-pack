# Redis and queue performance

Observe command latency, key volume, memory, eviction, hot keys, queue age and worker throughput.

Avoid wildcard scans.

Tune BullMQ concurrency against downstream DB/API capacity.

High worker throughput is not useful if it overloads dependencies.
