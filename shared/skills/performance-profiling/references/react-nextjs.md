# React and Next.js profiling

Inspect server/client boundaries, bundle composition and request waterfalls.

Avoid broad client components.

Profile expensive renders before adding memoization.

Use route-level bundle/analyzer tooling when available.

Check whether independent server requests can run in parallel.
