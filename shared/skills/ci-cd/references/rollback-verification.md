# Rollback and verification

Define what happens if:
- deploy fails
- health checks fail
- migration succeeds but app fails
- external integration breaks

Use post-deploy smoke/health checks.

Understand when forward-fix is safer than rollback, especially after data migrations.
