# Environments and promotion

Define dev/test/staging/production responsibilities.

Environment-specific configuration belongs outside built source.

Promote known artifacts where practical.

Record revision, migration version and deployment result.

Preview environments should not accidentally connect to production data/services.
