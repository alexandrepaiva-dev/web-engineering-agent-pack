# CI secrets and permissions

Use environment/repository secret stores.

Scope cloud/provider credentials to the minimum environment/action.

Prefer short-lived federated credentials when available.

Never print secrets.

Protect production deploy environments with appropriate approval/policy controls.
