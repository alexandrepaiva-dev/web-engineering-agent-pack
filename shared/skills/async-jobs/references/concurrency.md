# Concurrency

Choose worker concurrency based on CPU, DB capacity, downstream API limits, memory and I/O profile.

High concurrency can create self-inflicted outages.

Use rate limiting where dependencies require it.
