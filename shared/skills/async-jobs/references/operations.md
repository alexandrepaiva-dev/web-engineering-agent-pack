# Queue operations

Observe waiting/active/delayed/failed counts, retry rate, processing duration, queue latency, stalls and oldest waiting age.

Use correlation IDs.

Graceful shutdown should stop new work, drain active jobs within a budget, close connections and exit cleanly.
