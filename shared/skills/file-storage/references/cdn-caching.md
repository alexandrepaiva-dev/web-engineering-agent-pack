# CDN and caching

Use immutable/versioned keys for assets that can be cached long-term.

For mutable/private objects, define cache headers carefully.

Do not accidentally cache authenticated private responses publicly.

Use signed CDN access where required by the privacy model.
