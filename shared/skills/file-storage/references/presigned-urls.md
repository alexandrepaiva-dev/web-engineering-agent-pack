# Presigned URLs

Presigned URLs must be:
- short-lived
- operation-specific
- object-specific
- permission-scoped

Authorize the user before creating the URL.

For uploads, constrain content length/type where provider support and security model allow.

Do not reuse broadly privileged signed URLs.
