# Object keys and access

Use opaque/generated object keys rather than user-controlled paths.

Store object ownership and purpose in durable application data when authorization depends on it.

Public assets and private user files should have distinct access policies.

Deleting a DB record does not automatically delete the object; define lifecycle explicitly.
