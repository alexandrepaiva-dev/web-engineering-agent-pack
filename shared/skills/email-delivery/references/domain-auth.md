# SPF, DKIM and DMARC

Configure provider/domain authentication intentionally.

Verify:
- SPF alignment/provider setup
- DKIM signing
- DMARC policy/alignment

Use subdomains when separation of sending streams improves operational control.

Treat DNS changes as deployment configuration requiring verification.
