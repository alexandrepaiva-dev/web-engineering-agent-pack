# Email templates

Templates should be versionable, deterministic and previewable.

Use escaped content by default.

Keep critical action URLs generated from trusted application origins.

Include text alternatives where appropriate.

Avoid business logic duplicated inside templates.
