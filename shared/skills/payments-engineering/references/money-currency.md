# Money and currency

Represent exact monetary values using integer minor units or an exact decimal strategy appropriate to provider/currency.

Never use binary floating point for exact accounting arithmetic.

Define rounding rules.

Store currency alongside amount.

Do not assume every currency has two decimal minor units.
