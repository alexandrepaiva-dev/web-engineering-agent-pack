# Node and Docker base upgrades

Check runtime support matrix for frameworks/dependencies.

Review native module compatibility.

For Docker base image changes, verify libc/distribution assumptions, CA certificates, users/permissions and runtime tooling.

Rebuild from a clean cache.
