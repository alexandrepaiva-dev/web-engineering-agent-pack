# Lockfile validation

Review unexpected major transitive changes.

Do not hand-edit lockfiles.

Use the repository package manager.

Run security/compatibility checks according to project policy.

Ensure CI and developer environments resolve the same intended dependency graph.
