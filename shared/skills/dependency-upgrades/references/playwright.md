# Playwright upgrades

Review browser version changes, locator/assertion behavior, config changes, fixture behavior and CI system dependencies.

Install/update required browsers using the project's normal method.

Run critical E2E flows in CI-like conditions.
