# Errors, loading and streaming

Expected domain failures should produce actionable UI. Unexpected failures should be logged safely with correlation context.

Place loading boundaries where partial rendering improves experience, not everywhere.

Skeletons should approximate final dimensions to reduce layout shift.
