# Next.js security

- Server Actions and route handlers are externally callable boundaries.
- Authenticate/authorize independently of hidden UI controls.
- Validate callback/redirect URLs.
- Keep secrets out of client bundles.
- Avoid sensitive data in serialized props and error messages.
- Use the framework/Auth.js CSRF model correctly.
