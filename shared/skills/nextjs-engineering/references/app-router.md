# App Router

Use route groups, nested layouts, loading boundaries, errors and not-found handling to express product structure.

Do not promote an entire route subtree to client rendering because one nested element is interactive.

Inspect the installed Next.js version before relying on behavior that changed across releases.
