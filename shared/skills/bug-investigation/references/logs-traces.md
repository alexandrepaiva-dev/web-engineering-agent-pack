# Logs and traces

Use correlation IDs and timestamps to reconstruct the failing path.

Compare successful and failing executions.

Look for first divergence, not only final exception.

Do not add sensitive debug logging.

Remove temporary noisy instrumentation after diagnosis unless it has ongoing operational value.
