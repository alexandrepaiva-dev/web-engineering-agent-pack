# Regression fix

The final fix should:
1. address root cause
2. be minimal enough to reason about
3. preserve intended neighboring behavior
4. include a regression test that fails before the fix when practical
5. pass affected broader checks

Search for sibling instances of the same bug pattern.
