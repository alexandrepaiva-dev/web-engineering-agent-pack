# Review: performance

Raise only plausible material costs: O(n²) on realistic collections, N+1 DB work, unbounded fetches, large common-route client bundles, sequential independent network calls, wildcard Redis operations and uncontrolled queue concurrency.
