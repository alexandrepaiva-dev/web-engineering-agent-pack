# Review: security

Look for authentication bypass, IDOR/object authorization gaps, tenant leaks, CSRF/open redirects, secret/token leakage, SSRF, injection, insecure recovery/OAuth linking, webhook verification errors and sensitive logs.

Trace concrete input-to-privilege paths.
