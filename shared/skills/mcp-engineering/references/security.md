# MCP security

MCP expands the agent's capabilities and therefore its blast radius.

Review filesystem roots, database roles, network destinations, write access, destructive operations, tenant boundaries, and output trust.

Treat production write access as critical risk and require explicit human authorization.

A publicly listed MCP server is not automatically security-audited.
