# Authentication

Never commit credentials.

Prefer OAuth for remote user-authorized services where supported.

For token-based services, reference environment variables or platform credential storage instead of literal token values.

Document required environment variable names without providing values.
