# Claude Code MCP

Claude Code supports stdio and remote HTTP MCP servers.

Project-shared servers live in `.mcp.json`. User-scoped servers are stored in the user's Claude configuration.

Use `${ENV_VAR}` expansion for sensitive values instead of committing literal credentials.

Project-scoped servers require approval before use.
