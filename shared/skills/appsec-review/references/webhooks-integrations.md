# Webhooks and integrations

Verify provider signatures before parsing/processing privileged meaning.

Protect against replay and duplicate delivery.

Use provider event IDs for durable dedupe when appropriate.

Treat third-party API responses as untrusted input at boundaries.

Do not expose provider credentials to clients.
