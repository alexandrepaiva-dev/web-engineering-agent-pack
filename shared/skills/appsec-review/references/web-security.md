# Browser/web security

Review XSS, CSRF, clickjacking/frame policy, open redirects, unsafe HTML rendering, insecure cookies and client-side secret exposure.

Never render untrusted HTML without a deliberate sanitization strategy.

Authorization must not depend on hidden buttons.

Validate redirect destinations against an allowlist or trusted relative URL policy.
