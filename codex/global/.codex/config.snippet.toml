# Merge this into your existing ~/.codex/config.toml if desired.
# Do not blindly replace your model/provider/MCP configuration.

[agents]
enabled = true
max_concurrent_threads_per_session = 6

# Optional defaults:
# default_subagent_model = "gpt-5.6-terra"
# default_subagent_reasoning_effort = "medium"
